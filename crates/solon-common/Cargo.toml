[package]
name = "solon-common"
version = "0.4.2"
edition = "2021"
description = "Shared types and utilities for Solon"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
